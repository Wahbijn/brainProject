# Neural Medical AI — Python dependencies for BrainTumor/brain_api.py
#
# Install with:
#   pip install -r BrainTumor/requirements.txt
#
# Optional: create a virtual environment first:
#   python -m venv venv
#   venv\Scripts\activate   (Windows)
#   source venv/bin/activate (macOS / Linux)

flask>=3.0.0
flask-cors>=4.0.0
torch>=2.0.0
torchvision>=0.15.0
monai>=1.3.0
Pillow>=10.0.0
numpy>=1.24.0
